"use strict";

const Geolocation = require('./geolocation-config');
const RestApi = require('./rest-api-config');
const Splunk = require('./splunk-config');

module.exports = {
  Geolocation,
  RestApi,
  Splunk
}
